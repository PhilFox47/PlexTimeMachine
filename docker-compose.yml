services:
  plex-time-machine:
    build: .
    image: plex-time-machine:latest
    container_name: plex-time-machine
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Innerhalb des Containers liegen Datenbank und Cover immer im Volume.
      PTM_DATABASE_URL: sqlite:////app/data/plex_time_machine.db
      PTM_COVER_DIR: /app/data/covers
    ports:
      - "8088:8080"
    volumes:
      - ./data:/app/data
